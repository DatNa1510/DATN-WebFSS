# ================================================================
# FSS Backend - Application Configuration
# ================================================================

# Server
server.port=8080

# ── DATABASE (PostgreSQL) ────────────────────────────────────────
spring.datasource.url=jdbc:postgresql://localhost:5432/fss_db
spring.datasource.username=postgres
spring.datasource.password=123456
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ── GMAIL SMTP ───────────────────────────────────────────────────
# HƯỚNG DẪN: Lấy App Password tại https://myaccount.google.com/apppasswords
# Yêu cầu: Bật 2-Step Verification trên tài khoản Google trước
# Tạo App Password → Chọn "Mail" → Copy mật khẩu 16 ký tự
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ndat45489@gmail.com
spring.mail.password=qyhskqwybrelmszu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# ── JWT ──────────────────────────────────────────────────────────
# Secret key phải dài ít nhất 256 bit (32 ký tự) cho HS256
# Thay bằng một chuỗi ngẫu nhiên mạnh trong production!
jwt.secret=FssSecretKey2024VeryLongSecretKeyForJWTTokenSigningMustBe256BitsMin
jwt.expiration-ms=900000
# 900000ms = 15 phút

# ── FRONTEND URL (cho link trong email) ──────────────────────────
app.frontend-url=http://localhost:5173

# ── GOOGLE OAUTH2 ───────────────────────────────────────────────
google.client-id=495536065770-6v2unf2jqa7cv18lb4jd7u7gjuucpq2c.apps.googleusercontent.com

# ── LOGGING ──────────────────────────────────────────────────────
logging.level.vn.fss=DEBUG
logging.level.org.springframework.security=WARN
